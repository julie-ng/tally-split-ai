- description: 'qngwubea — five struck-through items plus margin initials (REWE receipt)'
  vars:
    caseDir: qngwubea-initials-inconsistent-strikethroughs
  # Will always run 3x, each counted separately. Not good for our, this fails 1 in 6.
  # options:
  #   repeat: 3
  assert:
    - type: javascript
      value: file://assertions/annotations-match-expected.mjs
